# Pull request

## What changed / why

## Evidence

- [ ] `python -m pytest` passes
- [ ] `ruff check src tests` and `ruff format --check src tests` pass
- [ ] New detectors include fixtures + evidence-graph tests (detections and false positives)
- [ ] Docs updated (`docs/`, `CHANGELOG.md` if user-facing)
- [ ] No secret values recorded, no network calls in the scan path, no fake capabilities
